# Copy to .env and fill in. The real .env is gitignored.
PORT=3458
# Long random string used to sign session cookies, e.g. `node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"`
SESSION_SECRET=replace-with-a-long-random-string
